{
  "name": "brandqube-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "BrandQube API — client logos and gallery assets stored in MongoDB (GridFS).",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "build": "echo build ok",
    "seed": "node scripts/seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongodb": "^6.12.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^9.0.3",
    "serverless-http": "^4.0.0"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
